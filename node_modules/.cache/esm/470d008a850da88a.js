let Koa,Router,bodyParser,mongoose,api;_850‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_850‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_850‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_850‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_850‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();





const { PORT, MONGO_URI } = process.env;



mongoose
 .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
  .then(() => {
      console.log("Conected to MongoDB")
  })
  .catch(e => {
      _850‍.g.console.error(e)
  });      

const app = new Koa();
const router = new Router();

// set up router
router.use('/api', api.routes());

// apply bodyParser
app.use(bodyParser());

// apply router on app
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;

app.listen(port, () => {
    _850‍.g.console.log('Listening to port %d', port);
});