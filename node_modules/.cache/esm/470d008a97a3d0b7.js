let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_97a‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_97a‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_97a‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_97a‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_97a‍.w("./api",[["default",["api"],function(v){api=v}]]);_97a‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();





const { PORT, MONGO_URI } = process.env;




mongoose
 .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
  .then(() => {
      console.log("Conected to MongoDB");
  })
  .catch(e => {
      _97a‍.g.console.error(e)
  });      

const app = new Koa();
const router = new Router();

// set up router
router.use('/api', api.routes());

// apply bodyParser
app.use(bodyParser());
app.use(jwtMiddleware);

// apply router on app
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;

app.listen(port, () => {
    _97a‍.g.console.log('Listening to port %d', port);
});